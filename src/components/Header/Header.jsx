import React from 'react';
import s from './Header.module.css';
const Header = function () {
    return (
        
        <header className={s.header}>
                <img src='https://logopond.com/assets/img/logo-footer.png' className={s.logo} />
            </header>
            );
            }
            
export default Header;